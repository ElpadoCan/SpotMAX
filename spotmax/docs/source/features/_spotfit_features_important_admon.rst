.. important::

    These features are available only if you set the parameter :confval:`Compute spots size (fit gaussian peak(s))` to ``True``. You will then find them in the files named ``_2_spotfit_`` (see the section :ref:`output-files`).